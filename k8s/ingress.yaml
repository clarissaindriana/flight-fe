apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flight-2306211660-fe-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - host: 2306211660-fe.hafizmuh.site
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: flight-2306211660-fe-service
                port:
                  number: 80